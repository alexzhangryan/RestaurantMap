.DS_Store
*.pyc
__pycache__/

# Secrets — never commit the API key
.env
.env.*

# Build output for the host — regenerated by `python3 build.py` (incl. in cloud build).
# The source photos live in photos/ (committed); deploy/photos is just a copy.
deploy/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# enrichment work products (regenerable caches / review artifacts)
details/
photos_cand/
sheets/
montages/

# favorites-sync + awards pipeline (regenerable)
live*.json
new_slugs.txt
awards_cache/
awards_review.log
